# 知识总结

> 本文档按提问逐步积累知识点，力求精简且能看懂。

## 目录

- [Java 反射机制](#java-反射机制)

---

## Java 反射机制

### 一句话定义

反射（Reflection）是 Java 提供的一种能力：**程序在运行时**，可以动态地获取任意一个类的内部信息（有哪些字段、方法、构造器、注解、父类、接口等），并且能**动态地创建对象、调用方法、读写字段**——即使这些成员是私有的。

### 关于你的理解（修正一下）

你的理解方向是对的，但有两点要纠正：

1. **主语不是 JVM，而是"你写的代码"。**
   不是"运行代码的家伙（JVM）"帮你去获取类信息，而是**你自己的 Java 代码**通过反射 API 主动去获取。JVM 只是提供了这个能力的底层支持。换句话说：反射是给程序员用的一套"运行时操作类"的工具。

2. **反射不只是"获取信息"，还能"操作"。**
   除了读取类的结构信息，反射还能：创建实例、调用方法、修改字段值。所以它不是只读的，而是能真正"动手"的。

### 为什么"运行时"这件事很关键

普通写法在**编译时**就必须写死类名、方法名：

```java
User u = new User();      // 编译时就确定了是 User 类
u.login();                // 编译时就确定了调用 login 方法
```

反射则可以在**运行时**才决定要操作哪个类、哪个方法（名字甚至可以来自配置文件、用户输入、网络数据）：

```java
// 类名是一个字符串，运行时才知道
Class<?> clazz = Class.forName("com.example.User");
Object obj = clazz.getDeclaredConstructor().newInstance(); // 创建对象
Method m = clazz.getDeclaredMethod("login");               // 拿到方法
m.invoke(obj);                                             // 调用它
```

这就是反射最核心的价值：**把"用哪个类/方法"这个决定，从编译期推迟到运行期。**

### 入口：Class 对象

每个被加载到 JVM 的类，都会对应一个 `Class` 类型的对象，它记录了这个类的全部结构信息。反射的一切操作都从拿到这个 `Class` 对象开始，三种常见方式：

```java
Class<?> c1 = User.class;              // 通过类字面量
Class<?> c2 = user.getClass();         // 通过已有对象
Class<?> c3 = Class.forName("com.example.User"); // 通过全限定类名字符串（最能体现"运行时动态"）
```

### 常用能力速查

| 想做的事 | 用到的类 / 方法 |
| --- | --- |
| 获取字段 | `getDeclaredFields()` / `getDeclaredField(name)` |
| 获取方法 | `getDeclaredMethods()` / `getDeclaredMethod(name, 参数类型...)` |
| 获取构造器 | `getDeclaredConstructors()` / `getDeclaredConstructor(...)` |
| 创建对象 | `constructor.newInstance(...)` |
| 调用方法 | `method.invoke(对象, 参数...)` |
| 读写字段 | `field.get(对象)` / `field.set(对象, 值)` |
| 访问私有成员 | 先 `setAccessible(true)` 绕过访问检查 |

> 注：`getXxx` 只能拿到 public 的（含继承来的）；`getDeclaredXxx` 能拿到本类声明的所有成员（含 private），但不含继承的。

### 它到底有什么用（实际场景）

反射是很多框架的基石，正因为框架在写的时候并不知道你以后会写哪些类：

- **Spring**：靠反射读取注解、创建 Bean、依赖注入。
- **JUnit**：靠反射找到并执行你标了 `@Test` 的方法。
- **JSON 序列化（Jackson/Gson）**：靠反射读写对象的字段。
- **JDBC / ORM（MyBatis、Hibernate）**：靠反射把数据库结果映射成对象。

### 代价（为什么不到处用）

1. **性能开销**：反射调用比直接调用慢（有额外的检查和查找）。
2. **破坏封装**：`setAccessible(true)` 能强行访问私有成员，容易破坏设计约束。
3. **失去编译期检查**：方法名、字段名写成字符串，写错了编译不报错，要到运行时才炸。

### 一句话总结

反射 = 让程序在运行时，把类当成"数据"来查看和操作的能力；代价是慢一点、不安全一点、错误更晚暴露。
