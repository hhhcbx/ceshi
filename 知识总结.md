# 知识总结

> 本文档按提问逐步积累。每个知识点保留原问题和当时的理解，只记录直接回答问题所需的内容；相关延伸在后续问到时再补充。

## 目录

- [Java 反射机制](#java-反射机制)
- [Java 注解](#java-注解)
- [Java Object 类](#java-object-类)
- [Java Lambda 与 Stream API](#java-lambda-与-stream-api)

---

## Java 反射机制

### 原问题

> Java 的反射机制是什么？我不要比喻，讲到我理解就行。

### 当时的理解

> 所谓反射机制就是在非编译阶段（即程序运行过程中），程序还是编译器还是 JVM，反正就是那个运行代码的家伙可以随意获取任何一个类的所有信息。

### 概念

反射（Reflection）是 Java 提供的一种能力：**你写的程序在运行时，可以动态获取类的结构信息，也可以动态创建对象、调用方法和读写字段。**

### 修正原有理解

方向基本正确，但要修正两点：

1. 主动使用反射的是**你写的 Java 程序**，不是编译器或 JVM。程序通过反射 API 发出操作，JVM 提供底层支持。
2. 反射不只能够“获取信息”，还能够“进行操作”，例如创建对象、调用方法和修改字段。

“可以随意获取任何信息”也不完全准确：反射受访问权限、Java 模块系统和安全限制影响，并非任何情况下都能访问所有内容。

### 为什么“运行时”很关键

普通写法在编译时就确定了类和方法：

```java
User user = new User();
user.login();
```

使用反射时，类名和方法名可以到运行时才确定：

```java
Class<?> clazz = Class.forName("com.example.User");
Object user = clazz.getDeclaredConstructor().newInstance();
Method method = clazz.getDeclaredMethod("login");
method.invoke(user);
```

因此，反射的核心价值是：**把“使用哪个类、调用哪个方法”的决定从编译期推迟到运行期。**

### 入口：Class 对象

每个被 JVM 加载的类，都对应一个 `Class` 对象。它记录了这个类的结构信息，反射操作通常从它开始：

```java
Class<?> c1 = User.class;                       // 已知类
Class<?> c2 = user.getClass();                  // 已有对象
Class<?> c3 = Class.forName("com.example.User"); // 运行时通过类名加载
```

---

## Java 注解

### 原问题

> Java 的注解是什么？我不需要知道底层原理，我想知道概念、怎么用。我现在的理解是注解就是把一些逻辑进行简化后的结果。可是我阅读代码时怎么知道某个注解的作用？同时比如我希望实现一个通用逻辑，我知道肯定有人做了注解，怎么知道去哪里找对应的注解？

### 当时的理解

> 注解就是把一些逻辑进行简化后的结果；需要通用逻辑时，应该已经有人做了对应的注解。

### 概念

注解（Annotation）是**附加在类、方法、字段、参数等代码元素上的结构化标记**，写法以 `@` 开头。它向编译器、框架或其他工具提供额外信息。

```java
@Override
public String toString() {
    return "User";
}
```

这里的 `@Override` 告诉编译器：这个方法应该重写父类方法。编译器会据此检查，但 `@Override` 本身没有执行任何业务逻辑。

### 修正原有理解

**注解不是“简化后的逻辑”，注解本身通常只是信息。** 真正读取这些信息并执行逻辑的是编译器、框架或工具。

例如：

```java
@GetMapping("/users")
public List<User> users() {
    return userService.list();
}
```

- `@GetMapping("/users")` 提供信息：这个方法处理 `/users` 的 GET 请求。
- Spring 读取该注解并完成请求映射。
- 如果没有 Spring 读取和处理它，这个注解不会自己执行任何操作。

所以更准确的关系是：

**注解负责声明信息，使用它的编译器或框架负责实现逻辑。**

通用逻辑也不一定由注解提供，还可能由普通方法、工具类、接口、中间件或配置提供。不要先假定“一定存在对应注解”。

缺少一个本应使用的注解时，提前写好的处理逻辑仍然存在，只是处理组件无法识别“这里需要这种处理”，因而不会把逻辑应用到这里。结果不一定是报错，也可能是：

- 编译失败或程序启动失败；
- 功能没有生效，例如事务或权限检查未执行；
- 程序仍能运行，但行为与预期不同。

### 怎么使用注解

使用注解时，先把它写在允许的位置；如果注解有参数，就按定义传入：

```java
@Deprecated
public void oldMethod() {}

@SuppressWarnings("unchecked")
public void convert() {}
```

使用框架注解时，还必须处于对应框架的运行环境中。例如 `@GetMapping` 只有被 Spring 管理时才有意义，不能只写上注解就期待它生效。

### 阅读代码时，怎么知道某个注解的作用

按以下顺序确认，不要只根据注解名字猜：

1. **在 IDE 中跳转到注解定义**：查看它的完整包名、注释和可填写的参数。
2. **判断它属于谁**：
   - `java.lang` 等 JDK 包：查 Java 官方文档。
   - `org.springframework...` 等依赖包：查对应框架的官方文档。
   - 当前项目包：搜索项目中的定义、使用位置，以及读取该注解的代码。
3. **查看注解所在位置和参数**：同一个注解标在类和方法上，作用范围可能不同。
4. **查官方文档或项目文档**：注解定义通常只能说明“能填什么”，实际何时生效、由谁处理，需要文档说明。

关键是同时弄清两个问题：**这个注解声明了什么信息？谁会读取并处理它？**

如果需要继续研究具体行为，应查找框架中的注解处理器、拦截器、切面或编译器插件，而不是只看注解定义。真正的处理代码不一定和注解位于同一个包中。

### 想实现某个通用功能时，去哪里找注解

不要直接搜索“有什么注解”，而要先确定**哪个组件负责这个功能**：

1. 明确需求，例如参数校验、权限控制、事务或接口映射。
2. 确认当前项目使用的 JDK 或框架是否负责该功能，例如 Spring MVC、Spring Security、Jakarta Validation。
3. 在该组件的官方文档中按功能搜索，查看推荐用法；如果方案使用注解，文档会给出对应注解。
4. 再在 IDE 的依赖库和项目现有代码中查找实际用例。

例如需要“校验参数不能为空”，应当先确认项目使用的是 Jakarta Validation，再查它的约束注解，找到 `@NotNull`；而不是在所有 Java 注解中漫无目的地搜索。

结论：**从功能找到负责它的组件，再从组件文档找到 API 或注解。不是所有通用功能都存在一个对应注解。**

### 最终理解

注解通常负责声明“这里需要某种处理”。编译器、框架或工具识别这个声明后，把已有逻辑应用到被标注的代码上。缺少注解时，已有逻辑并未消失，只是通常不会被应用。

了解一个注解时，应先查看官方文档，确定其作用、生效条件和处理者；需要深入研究时，再查真正读取该注解的处理代码。

---

## Java Object 类

### 原问题

> Java Object 类是所有类的超类，默认提供 11 个核心方法，具体有哪些？挑几个常用的或者关键的展开讲，其他一两句话带过。

### 当时的理解

> `Object` 是所有 Java 类的超类，并提供了 11 个核心方法。

### 11 个方法

传统所说的 11 个方法，是把 `wait()` 的三个重载分别计数：

1. `getClass()`
2. `hashCode()`
3. `equals(Object obj)`
4. `clone()`
5. `toString()`
6. `notify()`
7. `notifyAll()`
8. `wait()`
9. `wait(long timeout)`
10. `wait(long timeout, int nanos)`
11. `finalize()`

严格来说，它们只有 8 组不同的能力。所有 Java 类都直接或间接继承 `Object`；接口和基本数据类型不属于这种情况。

### equals()：判断对象是否相等

`==` 判断两个引用是否指向同一个对象；`equals()` 用来表达业务意义上的“内容是否相等”。

`Object` 默认实现仍然判断是否为同一个对象。希望按字段内容比较时，需要由具体类重写 `equals()`。

### hashCode()：生成哈希值

它主要用于 `HashMap`、`HashSet` 等哈希容器。必须遵守：

> 如果两个对象通过 `equals()` 判断相等，它们的 `hashCode()` 必须相同；哈希值相同的对象不一定相等。

因此，重写 `equals()` 时必须同时重写 `hashCode()`，否则对象在哈希容器中可能出现不符合预期的查找或去重结果。

### toString()：生成对象的字符串表示

打印对象、字符串拼接或记录日志时会调用 `toString()`。默认结果通常类似 `com.example.User@3b07d329`，自定义类一般会重写它，以便直接看到关键字段。

### getClass()：获取运行时类型

`getClass()` 返回对象实际所属的类，而不是变量声明的类型：

```java
Animal animal = new Dog();
animal.getClass(); // Dog.class
```

它也是反射的入口之一。

### 其他方法

- `clone()`：复制对象，默认是浅拷贝且使用限制较多，通常更推荐复制构造器或专门的复制方法。
- `wait()`：让当前线程在该对象上等待，调用时必须持有该对象的监视器锁。
- `notify()`：唤醒一个在该对象上等待的线程。
- `notifyAll()`：唤醒所有在该对象上等待的线程。实际开发通常优先使用更高级的并发工具。
- `finalize()`：对象被回收前可能执行的清理方法，已经废弃并计划移除，不应继续使用。

---

## Java Lambda 与 Stream API

### 原问题

> Lambda 表达式怎么用？用在哪？具体语法是什么？Java 8 的 Stream API 又怎么用？

后续追问：

> `new Thread(...).start()` 中的 `start()` 是什么语法？为什么和 Stream 一样使用 `.xx()`？Stream 只能处理数据吗？函数式接口通常只能使用 Java 提供的吗，可以自定义吗？

### Lambda 是什么

Lambda 是**简写函数式接口实现对象**的语法。函数式接口只有一个抽象方法，因此可以用一段 Lambda 直接提供该方法的实现。

```java
@FunctionalInterface
interface Calculator {
    int calculate(int a, int b);
}

Calculator add = (a, b) -> a + b;
int result = add.calculate(1, 2);
```

Lambda 不能脱离函数式接口独立存在。Java 提供了 `Predicate`、`Function`、`Consumer`、`Supplier` 等常用接口，也可以根据业务需要自定义；`@FunctionalInterface` 可以让编译器检查接口是否符合要求。

### Lambda 语法和用途

```java
(参数) -> 表达式

(参数) -> {
    多条语句;
    return 返回值;
}
```

Lambda 主要用于把一段行为传给方法，例如启动线程、指定排序规则、遍历集合或向 Stream 提供处理规则：

```java
new Thread(() -> System.out.println("执行任务")).start();
users.sort((u1, u2) -> u1.getAge() - u2.getAge());
```

### `.xx()` 与链式调用

`start()` 是 `Thread` 的普通实例方法，不是特殊语法。下面两种写法等价：

```java
new Thread(task).start();

Thread thread = new Thread(task);
thread.start();
```

`对象.方法()` 表示调用该对象的方法。如果一个方法返回对象，就能继续调用返回对象的方法，形成链式调用：

```java
对象.方法1().方法2().方法3();
```

链式调用是 Java 的通用写法，Stream、Builder、`StringBuilder`、`Optional` 等 API 都可能使用。看到连续的 `.xx()`，不能直接判断它是 Stream，应查看起始对象及每一步的返回类型。

### Stream API

Stream 用于连续处理一系列元素，本身不负责存储数据。数据可以来自集合、数组、文件或动态生成的序列。

```java
List<Integer> result = numbers.stream()
        .filter(n -> n % 2 == 0)       // 筛选
        .map(n -> n * n)               // 转换
        .sorted()                      // 排序
        .collect(Collectors.toList()); // 产生结果
```

一个 Stream 流程通常包括：

1. 创建 Stream，例如 `collection.stream()`；
2. 使用 `filter`、`map`、`sorted` 等中间操作描述处理过程；
3. 使用 `collect`、`forEach`、`count` 等终止操作产生结果。

Stream 的方法通常接收函数式接口，所以经常用 Lambda 提供具体处理规则。两者的关系是：**Lambda 描述一段处理行为，Stream 把多段处理行为连接成数据处理流程。**
