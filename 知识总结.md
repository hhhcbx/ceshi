# 知识总结

> 本文档按提问逐步积累。每个知识点保留原问题和当时的理解，只记录直接回答问题所需的内容；相关延伸在后续问到时再补充。

## 目录

- [Java 反射机制](#java-反射机制)
- [Java 注解](#java-注解)
- [Java Object 类](#java-object-类)
- [Java Lambda 与 Stream API](#java-lambda-与-stream-api)
- [Spring 基础与代码阅读](#spring-基础与代码阅读)

---

## Java 反射机制

### 原问题

> Java 的反射机制是什么？我不要比喻，讲到我理解就行。

### 当时的理解

> 所谓反射机制就是在非编译阶段（即程序运行过程中），程序还是编译器还是 JVM，反正就是那个运行代码的家伙可以随意获取任何一个类的所有信息。

### 概念

反射（Reflection）是 Java 提供的一种能力：**你写的程序在运行时，可以动态获取类的结构信息，也可以动态创建对象、调用方法和读写字段。**

### 修正原有理解

方向基本正确，但要修正两点：

1. 主动使用反射的是**你写的 Java 程序**，不是编译器或 JVM。程序通过反射 API 发出操作，JVM 提供底层支持。
2. 反射不只能够“获取信息”，还能够“进行操作”，例如创建对象、调用方法和修改字段。

“可以随意获取任何信息”也不完全准确：反射受访问权限、Java 模块系统和安全限制影响，并非任何情况下都能访问所有内容。

### 为什么“运行时”很关键

普通写法在编译时就确定了类和方法：

```java
User user = new User();
user.login();
```

使用反射时，类名和方法名可以到运行时才确定：

```java
Class<?> clazz = Class.forName("com.example.User");
Object user = clazz.getDeclaredConstructor().newInstance();
Method method = clazz.getDeclaredMethod("login");
method.invoke(user);
```

因此，反射的核心价值是：**把“使用哪个类、调用哪个方法”的决定从编译期推迟到运行期。**

### 入口：Class 对象

每个被 JVM 加载的类，都对应一个 `Class` 对象。它记录了这个类的结构信息，反射操作通常从它开始：

```java
Class<?> c1 = User.class;                       // 已知类
Class<?> c2 = user.getClass();                  // 已有对象
Class<?> c3 = Class.forName("com.example.User"); // 运行时通过类名加载
```

---

## Java 注解

### 原问题

> Java 的注解是什么？我不需要知道底层原理，我想知道概念、怎么用。我现在的理解是注解就是把一些逻辑进行简化后的结果。可是我阅读代码时怎么知道某个注解的作用？同时比如我希望实现一个通用逻辑，我知道肯定有人做了注解，怎么知道去哪里找对应的注解？

### 当时的理解

> 注解就是把一些逻辑进行简化后的结果；需要通用逻辑时，应该已经有人做了对应的注解。

### 概念

注解（Annotation）是**附加在类、方法、字段、参数等代码元素上的结构化标记**，写法以 `@` 开头。它向编译器、框架或其他工具提供额外信息。

```java
@Override
public String toString() {
    return "User";
}
```

这里的 `@Override` 告诉编译器：这个方法应该重写父类方法。编译器会据此检查，但 `@Override` 本身没有执行任何业务逻辑。

### 修正原有理解

**注解不是“简化后的逻辑”，注解本身通常只是信息。** 真正读取这些信息并执行逻辑的是编译器、框架或工具。

例如：

```java
@GetMapping("/users")
public List<User> users() {
    return userService.list();
}
```

- `@GetMapping("/users")` 提供信息：这个方法处理 `/users` 的 GET 请求。
- Spring 读取该注解并完成请求映射。
- 如果没有 Spring 读取和处理它，这个注解不会自己执行任何操作。

所以更准确的关系是：

**注解负责声明信息，使用它的编译器或框架负责实现逻辑。**

通用逻辑也不一定由注解提供，还可能由普通方法、工具类、接口、中间件或配置提供。不要先假定“一定存在对应注解”。

缺少一个本应使用的注解时，提前写好的处理逻辑仍然存在，只是处理组件无法识别“这里需要这种处理”，因而不会把逻辑应用到这里。结果不一定是报错，也可能是：

- 编译失败或程序启动失败；
- 功能没有生效，例如事务或权限检查未执行；
- 程序仍能运行，但行为与预期不同。

### 怎么使用注解

使用注解时，先把它写在允许的位置；如果注解有参数，就按定义传入：

```java
@Deprecated
public void oldMethod() {}

@SuppressWarnings("unchecked")
public void convert() {}
```

使用框架注解时，还必须处于对应框架的运行环境中。例如 `@GetMapping` 只有被 Spring 管理时才有意义，不能只写上注解就期待它生效。

### 阅读代码时，怎么知道某个注解的作用

按以下顺序确认，不要只根据注解名字猜：

1. **在 IDE 中跳转到注解定义**：查看它的完整包名、注释和可填写的参数。
2. **判断它属于谁**：
   - `java.lang` 等 JDK 包：查 Java 官方文档。
   - `org.springframework...` 等依赖包：查对应框架的官方文档。
   - 当前项目包：搜索项目中的定义、使用位置，以及读取该注解的代码。
3. **查看注解所在位置和参数**：同一个注解标在类和方法上，作用范围可能不同。
4. **查官方文档或项目文档**：注解定义通常只能说明“能填什么”，实际何时生效、由谁处理，需要文档说明。

关键是同时弄清两个问题：**这个注解声明了什么信息？谁会读取并处理它？**

如果需要继续研究具体行为，应查找框架中的注解处理器、拦截器、切面或编译器插件，而不是只看注解定义。真正的处理代码不一定和注解位于同一个包中。

### 想实现某个通用功能时，去哪里找注解

不要直接搜索“有什么注解”，而要先确定**哪个组件负责这个功能**：

1. 明确需求，例如参数校验、权限控制、事务或接口映射。
2. 确认当前项目使用的 JDK 或框架是否负责该功能，例如 Spring MVC、Spring Security、Jakarta Validation。
3. 在该组件的官方文档中按功能搜索，查看推荐用法；如果方案使用注解，文档会给出对应注解。
4. 再在 IDE 的依赖库和项目现有代码中查找实际用例。

例如需要“校验参数不能为空”，应当先确认项目使用的是 Jakarta Validation，再查它的约束注解，找到 `@NotNull`；而不是在所有 Java 注解中漫无目的地搜索。

结论：**从功能找到负责它的组件，再从组件文档找到 API 或注解。不是所有通用功能都存在一个对应注解。**

### 最终理解

注解通常负责声明“这里需要某种处理”。编译器、框架或工具识别这个声明后，把已有逻辑应用到被标注的代码上。缺少注解时，已有逻辑并未消失，只是通常不会被应用。

了解一个注解时，应先查看官方文档，确定其作用、生效条件和处理者；需要深入研究时，再查真正读取该注解的处理代码。

---

## Java Object 类

### 原问题

> Java Object 类是所有类的超类，默认提供 11 个核心方法，具体有哪些？挑几个常用的或者关键的展开讲，其他一两句话带过。

### 当时的理解

> `Object` 是所有 Java 类的超类，并提供了 11 个核心方法。

### 11 个方法

传统所说的 11 个方法，是把 `wait()` 的三个重载分别计数：

1. `getClass()`
2. `hashCode()`
3. `equals(Object obj)`
4. `clone()`
5. `toString()`
6. `notify()`
7. `notifyAll()`
8. `wait()`
9. `wait(long timeout)`
10. `wait(long timeout, int nanos)`
11. `finalize()`

严格来说，它们只有 8 组不同的能力。所有 Java 类都直接或间接继承 `Object`；接口和基本数据类型不属于这种情况。

### equals()：判断对象是否相等

`==` 判断两个引用是否指向同一个对象；`equals()` 用来表达业务意义上的“内容是否相等”。

`Object` 默认实现仍然判断是否为同一个对象。希望按字段内容比较时，需要由具体类重写 `equals()`。

### hashCode()：生成哈希值

它主要用于 `HashMap`、`HashSet` 等哈希容器。必须遵守：

> 如果两个对象通过 `equals()` 判断相等，它们的 `hashCode()` 必须相同；哈希值相同的对象不一定相等。

因此，重写 `equals()` 时必须同时重写 `hashCode()`，否则对象在哈希容器中可能出现不符合预期的查找或去重结果。

### toString()：生成对象的字符串表示

打印对象、字符串拼接或记录日志时会调用 `toString()`。默认结果通常类似 `com.example.User@3b07d329`，自定义类一般会重写它，以便直接看到关键字段。

### getClass()：获取运行时类型

`getClass()` 返回对象实际所属的类，而不是变量声明的类型：

```java
Animal animal = new Dog();
animal.getClass(); // Dog.class
```

它也是反射的入口之一。

### 其他方法

- `clone()`：复制对象，默认是浅拷贝且使用限制较多，通常更推荐复制构造器或专门的复制方法。
- `wait()`：让当前线程在该对象上等待，调用时必须持有该对象的监视器锁。
- `notify()`：唤醒一个在该对象上等待的线程。
- `notifyAll()`：唤醒所有在该对象上等待的线程。实际开发通常优先使用更高级的并发工具。
- `finalize()`：对象被回收前可能执行的清理方法，已经废弃并计划移除，不应继续使用。

---

## Java Lambda 与 Stream API

### 原问题

> Lambda 表达式怎么用？用在哪？具体语法是什么？Java 8 的 Stream API 又怎么用？

后续追问：

> `new Thread(...).start()` 中的 `start()` 是什么语法？为什么和 Stream 一样使用 `.xx()`？Stream 只能处理数据吗？函数式接口通常只能使用 Java 提供的吗，可以自定义吗？

### Lambda 是什么

Lambda 是**简写函数式接口实现对象**的语法。函数式接口只有一个抽象方法，因此可以用一段 Lambda 直接提供该方法的实现。

```java
@FunctionalInterface
interface Calculator {
    int calculate(int a, int b);
}

Calculator add = (a, b) -> a + b;
int result = add.calculate(1, 2);
```

Lambda 不能脱离函数式接口独立存在。Java 提供了 `Predicate`、`Function`、`Consumer`、`Supplier` 等常用接口，也可以根据业务需要自定义；`@FunctionalInterface` 可以让编译器检查接口是否符合要求。

### Lambda 语法和用途

```java
(参数) -> 表达式

(参数) -> {
    多条语句;
    return 返回值;
}
```

Lambda 主要用于把一段行为传给方法，例如启动线程、指定排序规则、遍历集合或向 Stream 提供处理规则：

```java
new Thread(() -> System.out.println("执行任务")).start();
users.sort((u1, u2) -> u1.getAge() - u2.getAge());
```

### `.xx()` 与链式调用

`start()` 是 `Thread` 的普通实例方法，不是特殊语法。下面两种写法等价：

```java
new Thread(task).start();

Thread thread = new Thread(task);
thread.start();
```

`对象.方法()` 表示调用该对象的方法。如果一个方法返回对象，就能继续调用返回对象的方法，形成链式调用：

```java
对象.方法1().方法2().方法3();
```

链式调用是 Java 的通用写法，Stream、Builder、`StringBuilder`、`Optional` 等 API 都可能使用。看到连续的 `.xx()`，不能直接判断它是 Stream，应查看起始对象及每一步的返回类型。

### Stream API

Stream 用于连续处理一系列元素，本身不负责存储数据。数据可以来自集合、数组、文件或动态生成的序列。

```java
List<Integer> result = numbers.stream()
        .filter(n -> n % 2 == 0)       // 筛选
        .map(n -> n * n)               // 转换
        .sorted()                      // 排序
        .collect(Collectors.toList()); // 产生结果
```

一个 Stream 流程通常包括：

1. 创建 Stream，例如 `collection.stream()`；
2. 使用 `filter`、`map`、`sorted` 等中间操作描述处理过程；
3. 使用 `collect`、`forEach`、`count` 等终止操作产生结果。

Stream 的方法通常接收函数式接口，所以经常用 Lambda 提供具体处理规则。两者的关系是：**Lambda 描述一段处理行为，Stream 把多段处理行为连接成数据处理流程。**

---

## Spring 基础与代码阅读

### 原问题

> 介绍一下 Spring，不要讲太底层，目的是理解机制、看得懂代码、会写会用。需要说清楚 IoC、Bean、常用注解和作用等。

后续主要追问了：

- 构造器注入、`@Autowired` 和其他注入方式；
- Bean 分层注解能否混用；
- 如何识别和使用陌生注解；
- 如何阅读微服务项目；
- 事务的概念和作用；
- Spring MVC 是什么。

### Spring、Spring Boot 与 Spring MVC

- **Spring Framework**：基础框架，核心能力是管理对象和依赖，并提供 Web、事务等模块。
- **Spring Boot**：建立在 Spring 之上，通过自动配置、Starter 和内置服务器简化项目配置与启动。
- **Spring MVC**：Spring 中负责处理 Web 请求的模块。

现代 Web 项目通常使用 Spring Boot 启动和配置应用，内部使用 Spring 的容器管理对象，再由 Spring MVC 处理 HTTP 请求。

### Spring 的整体运行机制

应用通常从这里启动：

```java
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

启动时，Spring 大致会：

1. 扫描启动类所在包及其子包；
2. 根据注解和配置找出需要管理的对象；
3. 创建这些对象并放入 IoC 容器；
4. 注入对象之间的依赖；
5. 根据配置启用 Web、事务等功能；
6. 启动内置 Web 服务器。

### Bean、IoC 与 DI

**Bean 是由 Spring 容器创建和管理的对象。** 自己通过 `new` 创建的普通对象默认不是 Bean。

```java
@Service
public class UserService {
}
```

Spring 扫描到 `@Service` 后，会创建并管理 `UserService` 对象。

**IoC（控制反转）**表示对象的创建和组装由 Spring 控制，而不是由业务代码到处使用 `new` 完成。存放和管理 Bean 的地方称为 IoC 容器，常见接口是 `ApplicationContext`。

**DI（依赖注入）**是 IoC 的具体实现方式：一个 Bean 需要另一个 Bean 时，由 Spring 将依赖传给它。

### 依赖注入的方式

推荐使用构造器注入：

```java
@Service
public class UserService {
    private final UserRepository repository;

    public UserService(UserRepository repository) {
        this.repository = repository;
    }
}
```

Spring 创建 `UserService` 时，会从容器中找到 `UserRepository` Bean 并传入构造器。类只有一个构造器时，不需要写 `@Autowired`。

`@Autowired` 的意思是：**从容器中找到符合要求的 Bean，注入到这里。** 它不负责注册 Bean。

另外两种方式是：

```java
// 字段注入：写法短，但依赖被隐藏，不利于测试，不推荐
@Autowired
private UserRepository repository;

// Setter 注入：适合可选或需要替换的依赖
@Autowired
public void setRepository(UserRepository repository) {
    this.repository = repository;
}
```

存在多个同类型 Bean 时，可使用 `@Qualifier` 指定名称，或使用 `@Primary` 标记默认选择。

### 注册 Bean 的常用注解

- `@Component`：通用组件。
- `@Service`：业务逻辑层组件。
- `@Repository`：数据访问层组件，还可能参与持久层异常转换。
- `@Controller`：返回页面的 Web 控制器。
- `@RestController`：通常返回 JSON 的 REST 控制器。
- `@Configuration`：配置类。
- `@Bean`：将配置方法返回的对象注册为 Bean，常用于第三方类或自定义创建过程。

`@Service`、`@Repository` 等都能注册 Bean，但不能因此随意混用。把 `UserService` 写成 `@Repository` 通常不会立即报错，业务代码也不会自动变成数据库代码；但它会误导读者，并可能触发针对持久层的附加行为、切面或检查规则。

### Spring MVC 是什么

MVC 是对 Web 代码职责的划分：

- **Model**：需要处理或展示的数据；
- **View**：最终显示的页面；
- **Controller**：接收请求、调用业务逻辑并决定返回什么。

Spring MVC 是 Spring 对这种 Web 开发模式的实现。它的核心流程是：

```text
HTTP 请求
  → DispatcherServlet 统一接收
  → 根据路径找到 Controller 方法
  → 解析路径、查询参数或 JSON 请求体
  → Controller 调用 Service
  → 返回页面，或者把对象转换成 JSON
```

`DispatcherServlet` 是统一入口，通常由 Spring Boot 自动配置，不需要开发者手动调用。

传统 MVC 会由 Controller 返回 View 和 Model。现在常见的前后端分离项目通常使用 `@RestController` 直接返回 JSON，虽然很少使用页面 View，但请求映射和参数解析仍由 Spring MVC 完成。

### MyBatis 与 JDBC

JDBC 是 Java 操作关系型数据库的标准 API。直接使用 JDBC 时，需要手动获取连接、设置 SQL 参数、处理 `ResultSet`、转换对象并释放资源。

MyBatis 是建立在 JDBC 之上的数据访问框架。开发者仍然控制 SQL，MyBatis 负责处理重复的 JDBC 工作、参数绑定、结果映射，并与 Spring 的 Bean 和事务机制集成。

```text
业务代码 → MyBatis → JDBC → 数据库驱动 → MySQL
```

MyBatis 没有取代 JDBC，也不会让开发者不再需要 SQL。它主要让数据访问代码从大量流程代码缩减为 Mapper 方法和 SQL。

### Spring MVC + MyBatis REST Demo

下面用“创建用户”和“查询用户”串起 Spring MVC、依赖注入、参数校验和 MyBatis：

```text
HTTP 请求 → Controller → Service → Mapper → MyBatis/JDBC → MySQL
```

数据库表：

```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    age INT NOT NULL
);
```

项目需要 Spring Web、Validation、MySQL Driver 和 MyBatis Starter。数据库连接放在 `application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: your-password

mybatis:
  configuration:
    map-underscore-to-camel-case: true
```

数据库对象：

```java
public class User {
    private Long id;
    private String name;
    private Integer age;
    // getter、setter
}
```

接口请求对象：

```java
public class CreateUserRequest {
    @NotBlank
    private String name;

    @NotNull
    @Min(0)
    private Integer age;
    // getter、setter
}
```

请求对象和数据库对象分开，避免接口输入直接控制持久化对象。

Mapper：

```java
@Mapper
public interface UserMapper {
    @Select("""
        SELECT id, name, age
        FROM users
        WHERE id = #{id}
        """)
    User findById(Long id);

    @Insert("""
        INSERT INTO users(name, age)
        VALUES(#{name}, #{age})
        """)
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(User user);
}
```

MyBatis 根据 Mapper 接口和 SQL 生成代理对象，并把它交给 Spring 管理。`#{...}` 用于安全绑定参数，`@Options` 将数据库生成的主键写回 `user.id`。

Service：

```java
@Service
public class UserService {
    private final UserMapper userMapper;

    public UserService(UserMapper userMapper) {
        this.userMapper = userMapper;
    }

    public User getById(Long id) {
        return userMapper.findById(id);
    }

    public User create(CreateUserRequest request) {
        User user = new User();
        user.setName(request.getName());
        user.setAge(request.getAge());
        userMapper.insert(user);
        return user;
    }
}
```

Controller：

```java
@RestController
@RequestMapping("/users")
public class UserController {
    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    @GetMapping("/{id}")
    public ResponseEntity<User> get(@PathVariable Long id) {
        User user = userService.getById(id);
        return user == null
                ? ResponseEntity.notFound().build()
                : ResponseEntity.ok(user);
    }

    @PostMapping
    public ResponseEntity<User> create(
            @Valid @RequestBody CreateUserRequest request) {
        User user = userService.create(request);
        return ResponseEntity.status(201).body(user);
    }
}
```

关键注解在流程中的作用：

- `@RestController`：注册 Controller Bean，并把返回对象转换成 JSON；
- `@RequestMapping`：设置类的基础路径；
- `@GetMapping`、`@PostMapping`：映射不同 HTTP 请求；
- `@PathVariable`：读取路径中的变量；
- `@RequestBody`：将 JSON 请求体转换成 Java 对象；
- `@Valid`：执行请求参数校验；
- `@Service`：注册业务层 Bean；
- `@Mapper`：让 MyBatis 为数据访问接口创建实现对象。

可以分别请求：

```text
POST /users
请求体：{"name":"张三","age":20}

GET /users/1
```

这个 Demo 的重点不是把所有代码都写在 Controller，而是看清每一层只负责自己的工作：Controller 处理 HTTP，Service 处理业务，Mapper 处理 SQL 和数据库访问。

### 配置和其他常用注解

- `@Value`：读取单个配置值。
- `@ConfigurationProperties`：把一组配置绑定成对象，通常更适合业务配置。
- `@ExceptionHandler`：处理指定异常。
- `@RestControllerAdvice`：集中处理多个 Controller 的异常。
- `@Transactional`：声明方法需要数据库事务。

配置通常位于 `application.yml` 或 `application.properties`，实际项目还可能按运行环境拆分配置。

### 如何识别陌生注解

注解不一定来自 Spring。阅读时先查看 `import`：

```text
org.springframework...     Spring
org.apache.ibatis...       MyBatis
jakarta.persistence...     JPA
jakarta.validation...      参数校验
lombok...                  Lombok
项目自己的包名              自定义注解
```

确认注解作用的顺序：

1. 在 IDE 中跳转到定义，查看完整包名、注释和参数；
2. 判断它属于哪个框架或项目模块；
3. 查看对应组件的官方文档；
4. 如果是自定义注解，搜索使用位置和读取它的处理代码。

自己写代码时，应从“要实现什么功能”出发查对应模块的文档，例如写 GET 接口就查 Spring MVC 的请求映射，而不是先背全部注解。项目中已有的相似代码可以作为入口，但仍要确认注解来源和含义。

### 数据库事务

事务把多个数据库操作视为一个整体：**要么全部成功并提交，要么失败后全部撤销。**

例如转账包含扣款和加款：

```java
@Transactional
public void transfer(Long fromId, Long toId, BigDecimal amount) {
    accountMapper.subtract(fromId, amount);
    accountMapper.add(toId, amount);
}
```

如果加款失败，事务会回滚已经执行的扣款，避免只扣款不加款。

事务通常具有 ACID 特性：

- 原子性：操作整体成功或整体失败；
- 一致性：事务前后数据满足业务约束；
- 隔离性：并发事务尽量避免错误影响；
- 持久性：提交后的结果被可靠保存。

Spring 通常通过代理在方法调用前开启事务，成功后提交，异常时回滚。默认主要对未处理的 `RuntimeException` 和 `Error` 回滚；需要对其他异常回滚时可以配置 `rollbackFor`。

本地事务只能管理当前事务管理器控制的资源。在微服务中，一个服务的 `@Transactional` 通常不能回滚另一个服务已经完成的 HTTP 请求，不能把本地事务当作整个微服务调用链的分布式事务。

### Spring Cloud 与微服务组件

单体应用把多个业务模块部署在一个进程中。微服务则把它们拆成可以独立启动和部署的应用，例如用户服务、订单服务和支付服务。拆分后会产生服务定位、通信、统一配置和故障处理等问题。

Spring Cloud 不是单个软件，而是一组帮助 Spring Boot 应用解决这些分布式问题的组件和编程模型。

#### 注册中心与服务发现

一个服务可能有多个地址会变化的实例。实例启动后把服务名、地址和健康状态登记到注册中心；调用方再根据服务名查询可用地址。

```text
user-service
├── 10.0.0.1:8080
└── 10.0.0.2:8080
```

常见注册中心有 Nacos、Eureka 和 Consul。Spring Cloud Alibaba 项目经常使用 Nacos。

#### 负载均衡

注册中心回答“有哪些可用实例”，负载均衡回答“这次选择哪个实例”。它把请求分散到多个实例，并避开已知不可用实例。Spring Cloud 现在常用 Spring Cloud LoadBalancer；旧项目中可能见到已经停止维护的 Ribbon。

#### 服务通信

不同服务运行在不同进程中，不能按普通 Bean 直接调用，通常使用 HTTP 或消息队列。

OpenFeign 可以把 HTTP 调用声明成 Java 接口：

```java
@FeignClient(name = "user-service")
public interface UserClient {
    @GetMapping("/users/{id}")
    UserResponse getUser(@PathVariable Long id);
}
```

调用这个接口时，Feign 生成 HTTP 请求，服务发现找到实例，负载均衡再选择一个地址。Feign 看起来像本地方法，但本质是网络调用，可能发生超时或服务不可用。

其他方式包括 `RestClient`、`WebClient`、RPC，以及用于异步通信的 Kafka、RocketMQ 等消息队列。

#### 配置中心

配置中心集中管理多个服务、多个实例和不同环境的配置，服务启动时按服务名和环境读取。常见方案有 Nacos Config、Spring Cloud Config 和 Consul KV。

数据库地址、超时时间和功能开关等适合集中配置；密码等敏感信息还需要配合密钥管理，不能仅因为进入配置中心就直接明文保存。

#### API Gateway

Gateway 是外部请求进入微服务集群的统一入口，通常负责路由、认证、限流、跨域和日志，而不是具体业务或数据库访问。

```text
客户端 → Gateway → user-service / order-service
```

Spring Cloud Gateway 路由示例：

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/users/**
```

`lb://user-service` 表示通过服务发现找到 `user-service`，再负载均衡选择一个实例。

#### 容错与可观测性

网络调用可能失败，因此还需要：

- 超时、重试、熔断和降级，防止故障扩散；
- 限流和隔离，避免单个服务耗尽系统资源；
- 日志、指标和链路追踪，定位请求经过哪些服务以及在哪里失败。

常见工具包括 Resilience4j、Sentinel、Micrometer 和 OpenTelemetry。

各组件解决的问题可以概括为：

| 问题 | 组件 |
| --- | --- |
| 服务在哪里 | 注册中心 |
| 多个实例选哪个 | 负载均衡 |
| 服务之间如何调用 | Feign、HTTP、消息队列 |
| 多个服务如何统一配置 | 配置中心 |
| 外部请求从哪里进入 | API Gateway |
| 服务故障如何控制影响 | 超时、熔断、降级、限流 |
| 如何定位跨服务问题 | 日志、指标、链路追踪 |

### 阅读 Spring 微服务项目

微服务中的每个服务通常都是独立应用，拥有自己的启动类、Spring 容器、配置、端口和 Bean。不同服务职责不同，不要求目录完全一致。

阅读步骤：

1. **划分服务**：搜索 `@SpringBootApplication`，结合 `spring.application.name` 找出可独立启动的应用。
2. **明确职责**：查看服务名称、依赖和配置，判断它是网关、业务服务还是公共模块。
3. **寻找入口**：业务服务从 `@RestController`、消息监听器或定时任务进入；Gateway 还要查看路由和过滤器配置。
4. **追踪一个请求**：从路径映射开始，依次追踪 Controller、Service、Mapper/Repository 和数据库。
5. **识别跨服务调用**：`@FeignClient`、`WebClient`、消息队列等意味着调用离开当前服务。
6. **查看构造器**：构造器参数基本就是一个类的依赖清单，可以快速看出它会访问哪些组件。
7. **核对数据对象**：区分请求对象、响应对象、数据库 Entity，以及不同对象之间的转换。

常见目录职责：

```text
controller       HTTP 入口
request          请求参数对象
response / vo    响应对象
service          业务逻辑
mapper           常见为 MyBatis 数据访问，也可能是对象转换
repository       数据访问
entity           持久化数据模型
client / feign   调用其他服务
config           配置
filter           请求过滤
exception        异常定义与处理
```

Gateway 可能只有路由、Filter、Controller 和 Request，因为它通常只负责转发、鉴权、限流等入口逻辑，不直接访问业务数据库。业务服务才更常见 Controller、Service、Mapper 和 Entity。

### 整体理解

Spring Boot 启动应用并完成自动配置；Spring 容器把组件创建成 Bean，再通过依赖注入组装它们；Spring MVC 把 HTTP 请求映射到 Controller；Controller 调用 Service，Service 再通过 MyBatis 等组件访问数据库；事务、校验和异常处理等通用能力由 Spring 在合适的位置执行。

拆成微服务后，每个服务仍然遵循这套内部结构，Spring Cloud 则进一步解决服务之间的发现、通信、配置、路由和容错问题。
